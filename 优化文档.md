# 设备连接模块优化文档

## 1. 问题描述

### 1.1 故障现象

设备连接失败，报错信息如下：

```
设备连接失败。[Could not find module '192.168.3.100:5005' (or one of its dependencies). Try using the full path with constructor syntax.]
```

### 1.2 影响范围

- 配置使用 TCP/IP 连接方式（如 `lockin_port = "192.168.3.100:5005"`）的设备无法正常连接
- 使用 USB 连接方式的设备不受影响

***

## 2. 问题分析

### 2.1 根本原因

代码在初始化设备连接时，**无法区分端口参数的格式**：

- 当用户配置 `lockin_port = "192.168.3.100:5005"`（TCP/IP格式）时
- 代码仍使用基于 USB 的 `LIA_API`（来自 `LIA_Mini_DoubleMW.py`）
- `USBController` 将端口参数当作 libusb 动态库路径，通过 `ctypes.CDLL()` 加载
- 系统尝试加载名为 `192.168.3.100:5005` 的 DLL 文件，导致失败

### 2.2 代码位置

- **文件**: `Exp_UI.py`
- **原始导入位置**: 第45行
- **设备连接位置**: 第865-873行

### 2.3 模块架构关系

```
┌─────────────────────────────────────────────────────────────────┐
│                       用户配置                                 │
│     lockin_port = "192.168.3.100:5005"  (TCP/IP)              │
│     lockin_port = "libusb-1.0.dll"       (USB)                 │
└─────────────────────────┬───────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────────┐
│                  get_lia_api(port)  [新增]                      │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 判断端口格式:                                           │   │
│  │ - IP:端口格式 → 使用 EthernetController                 │   │
│  │ - DLL格式     → 使用 USBController                      │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────┬───────────────────────────────────────┘
                          │
          ┌───────────────┴───────────────┐
          ▼                               ▼
┌─────────────────────┐       ┌─────────────────────────────┐
│ LIA_Mini_DoubleMW   │       │ LIA_Mini_DoubleMW_Ethernet  │
│ (USBController)     │       │ (EthernetController)        │
└─────────────────────┘       └─────────────────────────────┘
```

***

## 3. 解决方案

### 3.1 新增函数：动态选择API实现

在 `Exp_UI.py` 中新增 `get_lia_api()` 函数：

```python
def get_lia_api(lockin_port):
    if ':' in lockin_port and lockin_port.replace('.', '').replace(':', '').replace('-', '').isdigit():
        from interface.Lockin.LIA_Mini_DoubleMW_Ethernet_20250717 import LIA_API
        return LIA_API
    elif '.dll' in lockin_port.lower() or '.so' in lockin_port.lower():
        from interface.Lockin.LIA_Mini_DoubleMW import LIA_API
        return LIA_API
    else:
        from interface.Lockin.LIA_Mini_DoubleMW import LIA_API
        return LIA_API
```

### 3.2 修改设备连接代码

将固定导入改为动态选择：

```python
# 修改前
self.dev = LIA_API(port=self.lockin_port)

# 修改后
LIA_API = get_lia_api(self.lockin_port)
self.dev = LIA_API(port=self.lockin_port)
```

### 3.3 修改内容汇总

| 文件          | 修改类型 | 修改位置    | 说明                        |
| ----------- | ---- | ------- | ------------------------- |
| `Exp_UI.py` | 新增函数 | 第45-54行 | 添加 `get_lia_api()` 动态选择函数 |
| `Exp_UI.py` | 修改逻辑 | 第871行   | 使用动态选择的 API 类             |

***

## 4. 验证结果

### 4.1 测试命令

```python
from Exp_UI import get_lia_api

# 测试IP格式
api1 = get_lia_api('192.168.3.100:5005')
print('IP格式端口选择的API类:', api1.__name__)
print('IP格式API的模块来源:', api1.__module__)

# 测试DLL格式
api2 = get_lia_api('interface/Lockin/usblib/module_64/libusb-1.0.dll')
print('DLL格式端口选择的API类:', api2.__name__)
print('DLL格式API的模块来源:', api2.__module__)

print('是否为不同的实现:', api1 is not api2)
```

### 4.2 测试结果

```
IP格式端口选择的API类: LIA_API
IP格式API的模块来源: interface.Lockin.LIA_Mini_DoubleMW_Ethernet_20250717
DLL格式端口选择的API类: LIA_API
DLL格式API的模块来源: interface.Lockin.LIA_Mini_DoubleMW
是否为不同的实现: True
```

### 4.3 验证结论

- ✅ IP格式端口正确选择了 `EthernetController` 实现
- ✅ DLL格式端口正确选择了 `USBController` 实现
- ✅ 两种连接方式使用了不同的实现类

***

## 5. 兼容性说明

| 端口配置格式               | 兼容情况  | 使用的模块                                 |
| -------------------- | ----- | ------------------------------------- |
| `192.168.3.100:5005` | ✅ 已修复 | `LIA_Mini_DoubleMW_Ethernet_20250717` |
| `libusb-1.0.dll`     | ✅ 正常  | `LIA_Mini_DoubleMW`                   |
| `/usr/lib/libusb.so` | ✅ 正常  | `LIA_Mini_DoubleMW`                   |

***

## 6. 版本信息

- **修复版本**: `2b79dbc173b4b104d1cde9f5cbc7fd5e11eaf35f`
- **修复日期**: 2026-05-13

